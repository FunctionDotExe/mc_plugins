# Release artifacts

- `function-plugin-1.5.2.jar` — current build, including bundled WorldEdit 7.4.4 and the safe
  hosting-panel restart flow for self-updates.
- `function-plugin-1.5.1.jar` — previous build.

Verify the download against the adjacent `.sha256` file before installing it in a Paper server's
`plugins/` directory.
