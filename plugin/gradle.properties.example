# Copy this file to gradle.properties (gitignored) and fill in your own machine's paths.
# Both lines are optional — only needed if your default JDK isn't on PATH, or your
# network requires a custom truststore (e.g. a corporate TLS-inspecting proxy).

# org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-17.0.18.8-hotspot
# org.gradle.jvmargs=-Djavax.net.ssl.trustStore=/path/to/your/cacerts -Djavax.net.ssl.trustStorePassword=changeit
